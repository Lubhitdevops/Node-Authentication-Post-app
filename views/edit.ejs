<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Edit Post</title>
  <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
  <style>
    /* Sky gradient background */
    .sky {
      position: fixed;
      top: 0;
      left: 0;
      width: 100%;
      height: 100%;
      background: linear-gradient(to top, #87ceeb, #ffffff);
      overflow: hidden;
      z-index: -5;
    }

    /* Clouds */
    .cloud {
      position: absolute;
      top: 15%;
      left: -400px;
      width: 400px;
      height: 200px;
      background: url("https://i.ibb.co/4f3S6dq/cloud.png") no-repeat;
      background-size: contain;
      opacity: 0.7;
      animation: moveClouds 80s linear infinite;
    }

    .cloud2 {
      top: 35%;
      width: 500px;
      animation-duration: 100s;
      opacity: 0.6;
    }

    .cloud3 {
      top: 55%;
      width: 300px;
      animation-duration: 70s;
      opacity: 0.8;
    }

    @keyframes moveClouds {
      from {
        left: -600px;
      }

      to {
        left: 110%;
      }
    }

    /* Birds */
    .bird {
      position: absolute;
      top: 25%;
      left: -100px;
      width: 80px;
      height: 80px;
      background: url("https://i.ibb.co/gM5y6Zy/bird.gif") no-repeat;
      background-size: contain;
      animation: flyBirds 50s linear infinite;
    }

    .bird2 {
      top: 50%;
      animation-duration: 70s;
      width: 100px;
      height: 100px;
    }

    @keyframes flyBirds {
      from {
        left: -150px;
      }

      to {
        left: 110%;
      }
    }
  </style>
</head>

<body class="min-h-screen relative">

  <!-- Sky Layer -->
  <div class="sky">
    <div class="cloud"></div>
    <div class="cloud cloud2"></div>
    <div class="cloud cloud3"></div>
    <div class="bird"></div>
    <div class="bird bird2"></div>
  </div>

  <!-- Logout Button -->
  <a href="/logout"
    class="fixed top-4 right-4 bg-red-500 hover:bg-red-600 rounded-md px-3 py-2 text-white text-sm shadow-md transition">
    Logout
  </a>

  <!-- Content Section -->
  <div class="min-h-screen p-8 flex flex-col items-center justify-center">

    <!-- Edit Post Card -->
    <div class="w-full max-w-md bg-white bg-opacity-90 rounded-2xl shadow-lg p-8 backdrop-blur-md">
      <p class="text-gray-500 mb-6 text-center">✏️ Edit your post below</p>

      <form method="post" action="/update/<%= post._id %>">
        <textarea placeholder="What's on your mind?"
          class="block p-3 outline-none w-full resize-none bg-gray-50 border-2 border-gray-300 focus:border-blue-500 rounded-md transition"
          rows="4" name="content"><%= post.content %></textarea>

        <input
          class="font-medium px-4 py-2 bg-yellow-500 text-black hover:bg-yellow-600 rounded-md mt-3 w-full shadow-md transition cursor-pointer"
          type="submit" value="Update Post">
      </form>
    </div>

  </div>

</body>

</html>
