<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Login User</title>
  <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
  <script src="https://cdn.jsdelivr.net/npm/tsparticles@2.11.1/tsparticles.bundle.min.js"></script>
  <style>
    body {
      margin: 0;
      height: 100vh;
      font-family: 'Inter', sans-serif;
      overflow: hidden;
      display: flex;
      justify-content: center;
      align-items: center;
      background: linear-gradient(to bottom, #004f7a, #002f4b);
    }

    /* Floating login card */
    .card {
      position: relative;
      z-index: 10;
      background: rgba(255,255,255,0.9);
      backdrop-filter: blur(12px);
      border-radius: 2rem;
      padding: 2.5rem;
      max-width: 380px;
      width: 100%;
      box-shadow: 0 20px 60px rgba(0,0,0,0.5);
      animation: float 6s ease-in-out infinite;
    }

    @keyframes float {
      0%,100%{transform:translateY(0);}
      50%{transform:translateY(-15px);}
    }

    input:focus { box-shadow:0 0 10px rgba(0,191,255,0.7); }

    .btn-gradient {
      background: linear-gradient(135deg,#00d4ff,#005f9e);
      background-size:200% 200%;
      transition: all 0.5s ease;
    }
    .btn-gradient:hover { background-position:right center; transform: scale(1.05); }

    #tsparticles {
      position: absolute;
      width: 100%;
      height: 100%;
      top: 0;
      left: 0;
      z-index: 1;
    }
  </style>
</head>
<body>

<div id="tsparticles"></div>

<div class="card">
  <% if (typeof message !== 'undefined') { %>
    <div class="bg-green-500 text-white p-3 rounded-lg mb-4 text-center font-semibold animate-pulse">
      <%= message %>
    </div>
  <% } %>

  <h2 class="text-2xl font-bold text-center text-gray-800 mb-6">Login User</h2>

  <form action="/login" method="post" class="space-y-5">
    <input type="email" name="email" placeholder="Email"
      class="w-full px-5 py-3 border border-gray-300 rounded-xl focus:outline-none focus:ring-2 focus:ring-blue-400 shadow-sm">

    <input type="password" name="password" placeholder="Password"
      class="w-full px-5 py-3 border border-gray-300 rounded-xl focus:outline-none focus:ring-2 focus:ring-blue-400 shadow-sm">

    <button type="submit"
      class="w-full btn-gradient text-white py-3 rounded-xl font-semibold shadow-lg transition-transform duration-300">
      Login
    </button>
  </form>

  <p class="mt-6 text-center text-gray-700 text-sm">
    Don't have an account? <a href="/" class="text-blue-400 font-semibold hover:underline">Register</a>
  </p>
</div>

<script>
tsParticles.load("tsparticles", {
  fpsLimit: 60,
  background: { color: "transparent" },
  particles: {
    number: { value: 30, density: { enable: true, area: 800 } },
    color: { value: "#ffffff" },
    shape: { type: "circle" },
    opacity: { value: 0.7 },
    size: { value: { min: 2, max: 6 } },
    move: { enable: true, speed: 1, direction: "top", straight: false, outModes: "out" },
  },
  interactivity: { detectsOn: "canvas", events: { onHover: { enable: false } } }
});
</script>

</body>
</html>
