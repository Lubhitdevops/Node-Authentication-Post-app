<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Upload Profile Picture</title>
  <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
  <style>
    body {
      margin: 0;
      min-height: 100vh;
      font-family: 'Inter', sans-serif;
      display: flex;
      justify-content: center;
      align-items: center;
      background: linear-gradient(to bottom, #87ceeb, #ffffff);
      overflow: hidden;
    }

    .upload-card {
      background: rgba(255, 255, 255, 0.9);
      backdrop-filter: blur(15px);
      border-radius: 2rem;
      padding: 2.5rem;
      max-width: 400px;
      width: 100%;
      box-shadow: 0 20px 60px rgba(0, 0, 0, 0.4);
    }

    .btn-gradient {
      background: linear-gradient(135deg, #00d4ff, #005f9e);
      background-size: 200% 200%;
      transition: all 0.5s ease;
    }

    .btn-gradient:hover {
      background-position: right center;
      transform: scale(1.05);
    }
  </style>
</head>

<body>
  <div class="upload-card">
    <h2 class="text-2xl font-bold text-center text-gray-800 mb-6">Upload Profile Picture</h2>

    <form action="/upload" method="post" enctype="multipart/form-data" class="space-y-4">
      <input type="file" name="image" class="w-full p-2 border rounded-md focus:outline-none focus:ring-2 focus:ring-blue-400">
      <button type="submit"
        class="w-full btn-gradient text-white py-2 rounded-xl font-semibold shadow-lg transition">Upload</button>
    </form>

    <p class="mt-4 text-center text-gray-700 text-sm">
      <a href="/profile" class="text-blue-500 font-semibold hover:underline">Back to Profile</a>
    </p>
  </div>
</body>

</html>
